services:
  tagmonolith:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: tagmonolith
    env_file:
      - .env
    ports:
      - "8080:8080"
    volumes:
      - /mnt/AnimeImages/:/Images
